{% extends "SorinShopBundle:Shop:base.html.twig" %}


{% block shop_content %}

    <h3 style="display: inline-block">Shop : <em>"{{ shop.name }}"</em> </h3>


    <div style="display: inline-block">
        <a href="{{ path('sorin_shop_set', {'id': shop.id}) }}" type="button" class="btn btn-info">Éditer</a>
    </div>


    <div style="clear: both; margin-top: 40px"></div>

    <div class="well">
        <h4>Adress</h4>
        <p>{{ shop.adress }}</p>
    </div>
{% endblock %}